name: "Build docker image"
on:
  push:
    branches:
      - mamoru

env:
  REPOSITORY: mamorufoundation/subnet-env-sniffer

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build and push
        uses: docker/build-push-action@v3
        with:
          context: .
          file: ./Dockerfile
          push: true
          build-args: |
            AVALANCHE_VERSION=v1.10.17-Subnet-EVM-master
            CURRENT_BRANCH=${{ github.head_ref || github.ref_name }}
            SUBNET_EVM_COMMIT=$(git rev-parse HEAD || :)
          tags: |
            ${{ env.REPOSITORY }}:${{ github.sha }}
          cache-to: type=local,dest=/tmp/docker-cache
          cache-from: type=local,src=/tmp/docker-cache,mode=max

